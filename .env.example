# Database URL for Prisma
# For local development, use a local path
DATABASE_URL="file:./prisma/dev.db"

# For production builds, this will be ignored as the app
# uses the configurable database path from user settings
