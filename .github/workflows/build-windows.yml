name: Build Windows App

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-2022
    timeout-minutes: 60
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci --force
      
      - name: Generate Prisma Client
        run: npx prisma generate --schema=./prisma/schema.prisma
      
      - name: Copy Prisma files to @prisma/client
        run: xcopy /E /I node_modules\.prisma node_modules\@prisma\client\.prisma
      
      - name: Build Windows installers
        run: npm run build:win:all
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Upload Windows x64 installer
        uses: actions/upload-artifact@v4
        with:
          name: windows-x64-installer
          path: release/*/Thaziri-Setup-*-x64.exe
          retention-days: 30
      
      - name: Upload Windows ia32 installer
        uses: actions/upload-artifact@v4
        with:
          name: windows-ia32-installer
          path: release/*/Thaziri-Setup-*-ia32.exe
          retention-days: 30
      
      - name: Upload Windows universal installer
        uses: actions/upload-artifact@v4
        with:
          name: windows-universal-installer
          path: |
            release/*/Thaziri-Setup-*.exe
            !release/*/Thaziri-Setup-*-x64.exe
            !release/*/Thaziri-Setup-*-ia32.exe
          retention-days: 30
      
      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release/*/Thaziri-Setup-*.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: List built files
        run: dir release\*\*.exe
